# 测试执行结果 - Playwright E2E - 2026-04-08

## 执行摘要

| 项目 | 数量 |
|------|------|
| 计划执行 | 89 |
| 已执行 | 88 |
| 通过 | 86 |
| 失败 | 1 |
| 波动 | 1 |
| 阻塞 | 0 |
| 跳过 | 1 |

- 最近一次执行时间：2026-04-08
- 最近一次执行命令：`bash scripts/run-playwright-with-report.sh e2e web/e2e/register-login-validation.spec.ts web/e2e/role-access-control.spec.ts web/e2e/settings-pages.spec.ts web/e2e/settings-routing.spec.ts web/e2e/search-card-interaction.spec.ts web/e2e/search-page-full.spec.ts`
- 执行环境：
  - PostgreSQL：复用本机 Homebrew 服务，`localhost:5432`
  - Redis：复用本机 Homebrew 服务，`localhost:6379`
  - Backend：`server/scripts/run-dev-app.sh`，`local` profile
  - Frontend：`pnpm exec vite --host 0.0.0.0 --strictPort`
- 产物：
  - Markdown 验证报告：`web/test-results/playwright-e2e-validation-report.md`
  - HTML 报告目录：`web/playwright-report/`
  - JSON 结果：`web/test-results/playwright-e2e-results.json`

## 本轮已验证修复

- 注册页前端校验、字段级错误展示、接口错误映射已生效，用户名/密码/空提交相关用例通过。
- `registerSession()` 本地会话建立稳定，权限页与设置页回归通过。
- 搜索页空查询已回到引导空态，不再默认展示全量列表。
- 技能卡片已支持键盘聚焦、回车打开、可见 focus 态。
- 搜索分页切页后滚顶行为在定向回归中通过。
- 搜索整包单独回归结果：`57 passed / 1 skipped`。

## 结果明细

| 用例ID | 标题 | 结果 | 备注 |
|--------|------|------|------|
| TC_REG_003 | 重复用户名注册提示已存在 | 失败 | 组合回归高压场景下仍被本地注册限流覆盖，页面返回“请求过于频繁，请稍后再试”，未稳定命中重复用户名提示 |
| TC_SEARCH_INTERACT_043 | 平板视口展示 2 列卡片布局 | 波动 | 在组合回归中首轮失败、retry 后通过；单独回归与搜索整包回归已通过，表现为搜索索引可见性窗口导致的偶发空结果 |
| TC_SEARCH_PAGE_007 | 上一页按钮首页禁用 | 跳过 | 用例本身标记为跳过 |

## 回归建议

- P0：单独处理 `TC_REG_003` 的环境限流问题。建议为重复用户名场景补测试专用账号创建能力，或在本地 profile 放宽匿名注册限流，避免被 429 覆盖真实断言。
- P1：保留 `TC_SEARCH_INTERACT_043` 的 retry 配置，并继续观察搜索索引在组合回归下的稳定性；当前更像环境时序问题而非页面功能缺陷。
- P1：如果要做发布前最终放行，建议再补一轮全量 `make test-e2e-frontend`，重点关注搜索种子准备耗时和本地注册限流。
